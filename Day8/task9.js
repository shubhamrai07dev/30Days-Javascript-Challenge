const propertyName = "hereDynamicpropertyerty";
const propertyValue = "This is a dynamic value";

const obj = {
  // Using computed property names
  [propertyName]: propertyValue,
  anotherpropertyerty: "This is a static value",
};

// Logging the object to the console
console.log(obj);
