const showProducts = (name, age = 18) => {
    return `greeting of the day ${name} uncle ji. Your age is ${age}`;
    
};

console.log(showProducts("parakh"));

console.log(showProducts("ravi", 40));
